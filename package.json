{
  "name": "nanoclaw-st-course",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "build": "tsc",
    "start": "pnpm build && node dist/src/main.js",
    "test": "pnpm build && node --test dist/test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
