# 项目渐进式复现工作守则

本文件是长期协作时的工作索引和规则摘要。它不能覆盖或降低
`/home/shitanli/ln_rebuild/项目渐进式复现课程规范.md` 中的硬性要求；每次
继续工作前，若两者冲突，始终以规范文件为准。

## 1. 三个目录（不可混淆）

- 原项目（只读分析）：`/home/shitanli/ln_rebuild/nanoclaw/nanoclaw`
- 重建项目（逐课实现）：`/home/shitanli/ln_rebuild/nanoclaw/nanoclaw-st`
- 教程资料：`/home/shitanli/ln_rebuild/nanoclaw/nanoclaw-st/tutorial`
- 最高优先级规范：`/home/shitanli/ln_rebuild/项目渐进式复现课程规范.md`

原项目只用于阅读、对照和验证思路，不为课程目的直接修改。重建项目不是
原项目的副本，而是按真实需求重新长出来的教学实现。

## 2. 硬性工作规则

1. 需求先于设计：先出现可描述的业务问题，再写当前最简单的代码；问题
   暴露后才重构、抽象或引入组件。
2. 禁止提前设计：没有当前用途时，不添加接口、抽象基类、Provider、
   Factory、Adapter、Repository、Hook、Plugin、EventBus、预留配置或空实现。
3. 预热阶段可以规划完整大纲，但只能生成第 1 课教案；正式教学一次只
   推进当前课，不提前生成后续教案或实现后续代码。
4. 每节课都要能运行和测试，尽量遵循“需求 → 测试 → 失败 → 最小实现
   → 通过 → 必要时重构”。测试是理解流程的工具，不为覆盖率制造噪声。
   学生已明确偏好每课一次提供当前课的完整代码和测试；失败观察可在开发中完成，
   不设为必须等待学生反馈的单独阶段。测试仍须由学生亲自运行并反馈。
5. 正式教学必须交互式推进：解释一个需求，生成对应测试和最小实现，讲
   清代码，学生阅读并运行测试，学生反馈和提问，整理长期记录，再继续。
6. 第一次遇到异步、并发、数据库、容器、框架或第三方库时，按“大白话
   → 具体例子 → 当前代码 → 正式术语 → 为什么现在需要”解释。
7. 学习过程必须写入 `tutorial/`：工作守则、教学大纲、当前教案、参考
   分析、学习记录、典型报错和学生的高价值理解。
8. Agent 不执行 `git commit`。到达提交节点时只能提示学生查看
   `git status`、`git diff`，给出 `git add` 和中文 commit message，由学生手动提交。
9. 与原项目最终架构冲突时，优先级为：学生理解、真实需求驱动、禁止提
   前设计、核心业务正确、当前可运行可测试、学习曲线、代码清晰、最后才是
   对原结构的忠实程度。

## 3. 教案编写规范（重中之重）

每节教案文件名为 `tutorial/教案/第XX课-课程名.md`，主体固定且只使用下列
五个一级部分：

1. **前言**：从真实场景产生动机。说明上一课已有能力、现在出现的新场景、
   旧方案的具体不足、本课要增加的能力；不能只从技术名词开始。
2. **主要内容**：先用通俗语言列出本课步骤和任务，再给出“输入 → 类/方法
   → 输出或下一个调用”的代码阅读链条；有多条链条要分开写。最后说明相对
   上一课新增的是链条、分支还是环节，并做适度抽象总结。
3. **技术要点**：必须覆盖以下视角（没有实际内容时明确写“本课尚未涉及”，
   不能为了完整而提前设计）：
   - 环境配置：首次建项目时说明运行时、VS Code 测试入口和可复制命令，
     但不把大量临时操作堆进教案；
   - 坐标：指出当前代码在整个系统的横向模块和纵向层次中的位置；
   - 代码：解释生成的类/函数职责、输入输出、调用关系；生产代码和测试
     都要有直观命名与函数级中文注释；
   - 数据：给出具体输入、输出和重要中间值，说明数据在内存、文件或数据库
     中的去向；
   - 状态：如果有生命周期，画状态图并回答谁创建、谁改变、保存在哪里；
   - 流程图：每课都必须有 Mermaid 图，展示新增的调用链、数据流或状态流；
   - 细节技术：只解释本课真正用到的语法、库 API、异步机制或设计取舍。
4. **测试与验收**：写清测什么、为什么测、输入、预期结果、测试证明了什么。
   测试数量以帮助理解为准。正式教学时在聊天中再补充 VS Code 点击位置、
   终端命令和临时错误处理。
5. **总结**：回答本课比上一课多了什么、旧设计暴露什么、采用什么办法、
   还留下什么真实问题给下一课。设计模式只能在问题出现后作为总结术语，
   不能反过来驱动本课内容。

教案要能作为代码阅读顺序和复习资料，不能只是任务清单。每个重要的数据
类要说明它和相似数据的区别；每个重要状态要说明状态所有者和持久化位置。

## 4. 正式教学的运行节奏

开始新课前重新读取本文件、`tutorial/教学大纲.md`、上一课教案、学习记录和
当前重建代码，确认学生学过什么以及本课不能提前使用什么。

达到可测试节点时，聊天中明确告诉学生：现在验证什么、在 VS Code 的 Testing
面板或 Terminal 中如何运行、应该看到什么、失败时需要提供哪段完整输出。
学生真正运行并反馈后，才进入下一段实现。课程完成还必须确认代码可运行、
核心测试通过、数据流和状态变化已解释、学习信息已记录，并提示手动 commit。

每次生成或修改当前课代码后，还必须在聊天中提供一份可点击的源码阅读顺序：

- 粒度以函数（或测试回调）为单位；
- 链接必须指向该函数的实际起始行；
- 每项简述函数职责、输入输出以及它调用谁/被谁调用；
- 按真实调用链排列；若测试链和运行链不同，则分别列出；
- 行号随代码变化后必须重新核对，不能沿用旧链接。

## 5. Git 规则

适合提交时使用以下提示格式，不代执行：

```text
请先运行 git status 和 git diff 检查改动。
建议：git add <本次课程相关文件>
建议提交信息：<描述新增的系统能力>
请由你手动执行 git commit。
```

commit 应对应明确的小目标，例如“实现最小单次对话流程”，而不是“更新代码”。

## 6. 当前项目概要

NanoClaw 是一个个人 AI 助手宿主：消息从聊天平台进入，宿主完成路由和权限
判断，把消息写入某个会话；隔离容器内的 agent-runner 调用模型并写出回复；
宿主轮询回复并投递回原平台。最终系统的核心对象是用户、消息群组、Agent
群组、会话、输入/输出消息和容器；核心设计是中心路由数据库、每会话双 SQLite
邮箱、容器隔离和可按需安装的渠道/Provider。

第 1 课刻意只做“输入一段文字，得到一段确定回复”的本地最小闭环，不安装
模型 SDK、数据库、渠道库或 Docker。之后的复杂度必须由新的需求逐步推动。

## 7. 目录地图

### 原项目

- `src/index.ts`：宿主启动编排、数据库、渠道、投递轮询和清理。
- `src/router.ts`：平台事件到消息群组、Agent 群组、会话和输入消息的路由。
- `src/session-manager.ts`：会话查找/创建、会话邮箱和消息写入。
- `src/delivery.ts`：读取输出邮箱、通过渠道投递、重试和投递状态。
- `src/container-runner.ts`：按会话启动和停止 Docker agent 容器。
- `src/db/`：中心数据库实体、迁移和查询。
- `src/channels/`：渠道适配器契约、注册和 Chat SDK 桥接。
- `container/agent-runner/src/poll-loop.ts`：容器内轮询输入、调用 Provider、写出回复。

### 重建项目当前

- `package.json`：第 1 课的最小 TypeScript 工程和命令。
- `pnpm-lock.yaml`：锁定第 1 课 TypeScript 开发依赖的实际版本。
- `tsconfig.json`：源码与测试的编译设置。
- `src/reply.ts`：判断 `@Andy` 触发词，读取/追加同一会话的用户正文数组，返回回复或 `null`。
- `src/history.ts`：读取和写入当前唯一会话的 JSON 文件；首次无文件时从空数组开始。
- `src/main.ts`：启动时恢复历史，逐行处理输入，有效消息后保存并输出回复。
- `test/reply.test.ts`：触发/忽略、多轮回顾和新会话边界测试。
- `test/persistence.test.ts`：启动两个独立 CLI 进程，验证 JSON 落盘与重启恢复。
- `conversation.json`：运行时生成的个人会话数据，已被 Git 忽略。

### 教程资料

- `tutorial/教学大纲.md`：15 课渐进路线和进度表。
- `tutorial/学习记录.md`：学生反馈、典型报错、测试经验和长期偏好。
- `tutorial/教案/第01课-最小单次对话.md`：第 1 课教案。
- `tutorial/教案/第02课-只响应真正发给助手的消息.md`：第 2 课教案。
- `tutorial/教案/第03课-记住本次对话的上一句.md`：第 3 课教案。
- `tutorial/教案/第04课-重启后仍记得对话.md`：当前课程教案。
- `tutorial/参考/`：原项目、流程、数据、状态、模块、依赖和取舍分析。

## 8. 当前进度

- 预热分析：已完成。
- 教程资料和课程大纲：已建立。
- 第 1 课教案：已建立。
- 第 1 课：学生已理解、测试、手动提交并上传，课程完成。
- 第 2 课：学生已运行命令行测试、手动提交并上传，课程完成。
- 第 3 课：学生已手动提交并请求下一课；以此视为完成，测试运行反馈未另行明说。
- 第 4 课：教案、测试和最小实现已写完，等待学生阅读、运行、反馈与验收。
- 第 5 课及以后：只有大纲条目，没有教案和实现代码。
- Git：`main` 已连接 `origin/main`；所有后续 commit 仍由学生手动执行。
